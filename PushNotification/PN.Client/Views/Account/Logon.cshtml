@model PN.WebAPI.Entities.User
@{
    Layout = null;
    ViewBag.Title = "Logon";
}

<!DOCTYPE html>
<html>
<head>
    <link href="~/css/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/css/Content/style.css" rel="stylesheet" />
</head>
<body class="login-page">
    <div class="login-box">
        <div class="card">
            <div class="body">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <b style="color:#a94442;" class="align-center">@TempData["invalidLogin"]</b>
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">@Html.LabelFor(x => x.username)</i>
                        </span>
                        <div class="form-line">
                            @Html.EditorFor(model => model.username, new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                        @Html.ValidationMessageFor(model => model.username, "", new { @class = "text-danger" })
                    </div>
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">@Html.LabelFor(x => x.password)</i>
                        </span>
                        <div class="form-line">
                            @Html.EditorFor(model => model.password, new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                        @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" })
                    </div>
                    <div class="row">
                        <div class="col-xs-offset-6 col-xs-6">
                            <button class="btn btn-block bg-pink waves-effect" type="submit">Logon</button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</body>
</html>

